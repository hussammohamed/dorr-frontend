import Route from '@ember/routing/route';

export default Route.extend({
    breadCrumb: {
        title: "كشف حساب",
        linkable: true,
        path: "index.properties.show.statements"
    },
});
